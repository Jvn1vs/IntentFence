# Experiment log

Append-only template. Do not edit old results after seeing a final test; add a new run with a new
data/model/policy version. A blank field is not a zero and does not prove that a run occurred.
Use the evidence vocabulary from `docs/claim_evidence_matrix.md`.

## Run ID

- Evidence status (`planned` / `smoke-tested` / `validated` / `reproduced` / `negative` / `not-run`):
- Date/time:
- Protocol version / approval state:
- Hypothesis / preregistered comparison:
- Run executor (project owner; Codex does not execute training):
- Git commit:
- Data manifest SHA-256:
- Calibration data SHA-256:
- Model and revision:
- Input mode:
- Config file/hash:
- Random seed:
- Hardware / OS:
- Python / PyTorch / Transformers / CUDA:
- Training duration / cost:
- Wall time / GPU-hours / actual CNY cost:
- Best epoch and checkpoint:
- Temperature (risk/alignment):
- Frozen thresholds / policy version:
- Validation metrics:
- Test metrics (one evaluation after freeze):
- Raw predictions:
- Error analysis:
- Deviations / known limitations:
- Final-test access count for this protocol:
- Claim IDs supported or contradicted:
- Artifact manifest path and SHA-256:
- Deviation record / reason for missing fields:
- External model/API used (provider, revision, material class, terms):
- Public-release approval status:
