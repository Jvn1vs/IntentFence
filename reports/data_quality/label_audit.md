# Label audit status

Status: **not yet completed on public datasets**.

The repository includes `scripts/audit_labels.py`, which creates a stratified CSV for at least 200 rows. Before a formal model run, replace this status with source-specific counts and noise estimates, field-boundary issues, ambiguity policy, reviewer procedure, agreement statistic where available, and a versioned correction log.

The synthetic smoke fixture was reviewed for schema/tests only and does not satisfy the public-dataset audit exit condition.
