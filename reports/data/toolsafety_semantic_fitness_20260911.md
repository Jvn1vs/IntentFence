# ToolSafety 语义适用性探查与下一阶段范围

日期：2026-09-11。状态：只读探查完成，不能据此声明五类风险已覆盖。

## 本轮证据

固定源和前次诊断哈希已重新验证。对全部 tool 消息的 value 作 casefold 后，检查以下字面子串：`ignore previous`、`ignore all previous`、`system prompt`、`system message`、`disregard`、`api key`、`administrator`、`override`。不作关键词标签映射。

205 个消息位置命中至少一个词，其中仅 15 个在之前通过有限结构筛查的 4760 个位置内，其余 190 个不在其中。这个探查不具备已知召回率，不代表只存在 15 个攻击，更不能将其他位置全部判为正常。

Codex/AI 完整阅读了 5 个动作前缀，涉及 4 条原对话。取样为前三个命中且完整前缀不超过 8000 字符的位置，再加 administrator 和 override 的筛查内命中；不是随机抽样。零基位置及判断：

| 原记录 / tool 消息位置 | 可用性观察，不是标签真值 |
|---|---|
| 4878 / 4、6 | 用户明确提出凭据生成及用于商品检索；凭据出现本身不足以证明外泄。两位置属于同一任务，工具信任和权限仍未验证 |
| 5241 / 2 | 用户要求从凭据库取钥并检查账户连通性；脱敏值被传入后续调用，其执行真实性仍有疑问 |
| 6971 / 2 | administrator 出现在职位名称中，不能映射为权限提升 |
| 7617 / 2 | override 位于普通语言选择解释中，不能映射为提示注入 |

未读取这些位置之后的返回或最终回答来给当前动作赋标签。另单独检查了唯一命中前四个更具体短语的消息位置 7559 / 10：它包含论坛中的危险话题叙述，后续是自然语言拒绝，不是工具调用；这只用于解释其为何不在动作入口内，没有产生训练样本或风险真值。

本轮没有确认 data_exfiltration、privilege_escalation、tool_manipulation 的可用样本数量。结论是 **ToolSafety 仍只能作为待审核工具上下文来源，不能当作缺失三类风险已经补齐的证据**。下一步应优先审核语义覆盖，而不是无止境地扩展解析规则。

## 审计记录

本地文件 `data/interim/toolsafety_audit_20260910/semantic_probe_20260911.json` 记录全部命中位置、方法、源哈希及五条 AI 观察的前缀哈希；SHA-256 为 `efe12f8d86b26b6dc6e48d0d769ff168321bd26a39fb3e3ffdb2a494796860b1`。该文件没有复制原始凭据或对话文本，Risk/Alignment 保持 null，human_verified=false。

只有报告和审计记录新增，没有更改解析器、训练配置或模型。本轮无需重跑代码测试；上一实现提交的 296 项测试结果仍仅适用于该实现。

## 具体待批准阶段：隔离语义复核包

当前所有者只批准了下载和只读审计，`configs/candidate_9_toolsafety_audit.yaml` 中候选接入仍为 false。建议下一阶段明确允许在独立隔离目录生成 **最多 200 条结构化动作前缀复核记录**，用于决定是否值得扩大转换；不合并到训练候选。

- 输入限定为上述固定 ToolSafety 快照及已通过有限结构检查的位置；每条对话最多一个位置，固定 seed=42，抽样方法和分层依据单独记录，不声称总体代表性。
- 不修复无效调用，不从冲突定义中择一。首批仅取唯一工具定义分组，完全相同的重复定义也先留待后续。
- 输出字段为原记录/消息索引、原目标、工具定义、截至当前动作的历史、拟执行动作、源与前缀哈希、条款/隔离检查状态和空白独立 Risk/Alignment 审核槽。不含当前动作之后的结果或最终回答。
- 对这批记录作有来源标记的 AI 语义预审，记录无法映射、任务边界不足与不确定项；不得设置 human_verified=true，不自动沿用来源的安全回应标签。
- 上游归属或与锁定校准/测试隔离未解决的记录保持隔离，不能成为训练输入；逐样本保留源 MIT 声明及 Glaive/ToolBench/ToolAlpaca 等已知来源线索，不把所有上游权利一概宣称厘清。
- 退出交付是有证据的类别覆盖、问题清单及继续/排除建议，不承诺补齐五类风险。无新下载、无 API/GPU 费用，不执行工具或模型，不发布数据，保留第一轮和 Dolly v3 证据。

此次阶段确认依据仓库 AGENTS.md 的逐阶段确认要求，以及已记录的“只读审计”授权边界；不是检索 skill 另设的审批。
