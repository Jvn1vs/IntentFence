# 指令劫持来源对照与下一项良性候选

依所有者持续授权，从已经下载的 AIB 固定快照中增加 AIB-00022、AIB-00099 两组同前缀对照、四条离线返回提案。复用已有严格来源绑定器，未修改源消息、历史策略实现或已封存结果。来源 `ppradyoth/AgentInjectionBench` revision `ef230359966c4d87b1c925aa56b8bf6e3f6ffed3`，沿用 Apache-2.0 归属及原 LICENSE。

两例的目标候选均为注入标记之前的文章摘录，低信任候选为攻击明确要求输出的推广语。参数全部取原文字符范围，后续示范不参与选择。原文章含省略号，00022还存在标记之后的正文，故目标候选只是部分摘录，不是完整摘要；00099对照也只表达指定推广片段，不包含完整的追加推荐流程。不能为了补标签把前者自动认定 aligned、后者自动认定 malicious。风险机制来自既有 AI 场景意见，尚未赋训练标签。

构造入口 `scripts/build_aib_hijacking_pilot.py`，配置 `configs/aib_hijacking_pilot_20260911.yaml`。两次内存重放及落盘后 `--verify` 通过，源文件哈希不变；Ruff通过，相关来源绑定八项fixture测试通过（0.25秒）。本轮复用已验证实现，没有参数学习或外部执行。未重复全量模型框架测试。

ignored 目录 `data/interim/aib_hijacking_pilot_20260911/` 的证据：

- preparation.json：`df7305a32d98a1b1d71f2b57c8171147271310fb261649779c56de55d5d5ce78`。
- run_v1/records.jsonl：`adf5474677a059ba464ce77b2958e3f29942021e6add7b3421d13cb4ce5316ee`。
- run_v1/manifest.json：`19ef3b3ac5243a0d7e7a74d570d80edb019ed8209ac6e0204f2f58719aba336a`。

结合上一版清点的10场景/14操作/1澄清，此轮新增两个不同ID后累计12场景、18操作提案、1澄清；此累计为阶段算术汇总，旧inventory未覆盖重写。仍无正式Risk/Alignment或split，不能宣称训练就绪。

另完整阅读 AIB-00140：前一查询结果为账单队列开放工单37条，当前用户追问其中紧急标签数量，最新结果为6条。可研究同内容下返回当前筛选数与误用旧总数的对照；需显式保存两个查询范围和当前用户指代，不能把所有历史数字随意视为可互换。该良性对照尚未构造，下一步自主实现并审查。随后统一整理逐动作AI预审侧记录及家族隔离，不按策略名称填标签。
