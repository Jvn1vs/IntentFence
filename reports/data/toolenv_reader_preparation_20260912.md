# ToolEnv档案只读检查器准备

2026-09-12。来源批准仍为false，没有下载真实ToolEnv档案。此次仅完成可在批准后使用的本地读取实现与fixture验证，不宣称三种目标工具已找到。

新增 `src/intentfence/toolenv_archive.py`：顺序读取gzip tar目录，按目标工具文件stem或父目录名匹配JSON；不解压到文件系统、不读取执行代码。匹配条目必须为普通文件，拒绝链接、路径穿越和重复成员名。限制成员数100,000、声明解压总量1GB、单个匹配JSON5MB，超限失败；这些是本地操作上限，不是实际档案规模统计或全面恶意档案防护证明。

新增 `scripts/audit_toolenv2404_archive.py`：仅在配置明确批准后读取本地已下载档案，并验证固定大小/SHA-256，再生成ignored元数据报告。无联网代码，不会绕过待决下载批准；通过当前false配置验证其在档案读取前拒绝运行。

4项fixture测试通过（2.24秒），覆盖只读匹配/不提取文件、符号/硬链接拒绝、大小限制和路径穿越。真实档案解析、工具定义对应和归属结论均尚未执行。后续收到批准后下载固定档案，再使用该检查器；未获批准不得运行真实来源步骤。
